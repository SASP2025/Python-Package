from setuptools import setup, find_packagessetup(    name='exercise_package',         # Name of your package    version='0.1',    packages=find_packages(),      # Automatically finds your python_package/    install_requires=[],           # Add any needed libraries like numpy here    extras_require={        "tests": ["pytest",                  "pytest-cov"        ]                        })